# Base directory: docs-site
# This is where Netlify installs dependencies and runs build command
base = "."

[build]
command = "bun run build"
publish = "dist"

# Environment variables
[context.production]
environment = { NODE_ENV = "production" }

[context.deploy-preview]
environment = { NODE_ENV = "production" }
